version: '3.8'

services:
  conmedrl-app:
    build:
      context: ../
      dockerfile: Docker-Deployment/Dockerfile
    container_name: conmedrl-dev
    ports:
      - "5000:5000"
    volumes:
      - ../:/app
      - /app/Docker-Deployment
    environment:
      - FLASK_APP=CDM-Software/web_application_demo.py
      - FLASK_ENV=development
      - FLASK_DEBUG=1
      - PYTHONPATH=/app
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - conmedrl-network

  # Optional: Add database service
  # conmedrl-db:
  #   image: postgres:13
  #   container_name: conmedrl-db
  #   environment:
  #     POSTGRES_DB: conmedrl
  #     POSTGRES_USER: conmedrl
  #     POSTGRES_PASSWORD: password
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   networks:
  #     - conmedrl-network

networks:
  conmedrl-network:
    driver: bridge

volumes:
  postgres_data:
    driver: local 