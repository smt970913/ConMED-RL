"""Compatibility entry point for legacy setuptools commands.

Package metadata, dependencies, and optional extras are defined in
``pyproject.toml``.
"""

from setuptools import setup


if __name__ == "__main__":
    setup()
